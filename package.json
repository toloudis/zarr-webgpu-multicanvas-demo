{
  "name": "zarr-webgpu-multicanvas-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "generate:data": "node scripts/generate-demo-zarr.mjs"
  },
  "dependencies": {
    "zarrita": "^0.7.3"
  },
  "devDependencies": {
    "vite": "^8.0.16"
  }
}
